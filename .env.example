NODE_ENV=development

# Worker polling interval in milliseconds (default: 2000)
# POLL_INTERVAL_MS=2000

# Web server base URL used by the worker to reach render pages (default: http://localhost:3000)
# WEB_URL=http://localhost:3000

# Port for the web server (default: 3000)
# PORT=3000
